# Optional: enables AI-generated social-share copy. Without it, RUNWAY uses a
# deterministic game-state draft so sharing still works locally and in previews.
OPENAI_API_KEY=
OPENAI_SHARE_MODEL=gpt-5.6

# Optional canonical URL used for absolute social metadata outside Vercel.
NEXT_PUBLIC_SITE_URL=
