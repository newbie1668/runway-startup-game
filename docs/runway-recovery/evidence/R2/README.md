# R2: startup hydration recovery

The lead reproduced React #418 before editing application code. The first HUD paragraph differed: frozen production HTML said `18:48 SAT · SEPT 5`; the client clock said `23:59 THU · JAN 31`. The injected client time was 2030-01-31T23:59:40Z. [Before evidence](before/result.json), [captured HTML](before/server.html), and [screenshot](before/clock-mismatch.png) preserve the mismatch. HTML SHA256: `0e3c6aecc831ab2c7091a13fe9dde579ac73beb2c6421a3c641042ea2c799b87`.

Before capture checkout: `b8724e4fb8a288ac6785629cfd77db3557142bd5`; production build from the prior recorded `31b1d992b4dce272e6a1f2596dc9992e5c316d98` has identical app, component, library, public asset and dependency content through b8724e4. This was real production React hydration, with WebGL2 deliberately unavailable so the existing automatic 2D fallback could isolate the clock error. It does not prove 3D readiness.

R2a now uses a cached clock timestamp, a neutral server/initial client snapshot, and one shared 30-second timer. Hooks stay unconditional. Monthly climate values are labelled typical. [Independent clock review](clock-review.md) includes timer cleanup and remount checks. Worker commits `5160fec` and `5c06dbd` were integrated as `05e04c2` and `db38a6d`.

The focused browser command is `RUNWAY_BASE_URL=http://127.0.0.1:4318 node scripts/test-map-hydration.mjs`. It freezes the server document and hydrates production JavaScript with a different client time, covers visible/hidden HUD and explicit 2D routes on desktop/mobile through reload, and advances across midnight and the month boundary. Its clock controls follow the [Playwright Clock API](https://playwright.dev/docs/clock). The WebGL2 capability stub is test-only and no rendering-performance claim may be drawn from this regression.

All four integration commands passed at `db38a6d8312c6468e3a3f27b980a60c1d4264e2e`: 111 game checks (187.2 seconds), 9 UI checks, lint and production build. [Command logs/results](integration-checks/results.json) preserve actual exits. R2b adds only the focused test runner after that build; fresh integration lint also passed at `1543b56`, with runtime/dependencies unchanged. [Runner review](browser-runner-review.md) passed after fixing the initial clock assertion race and locator timeouts. The final 12-case browser result is pending. G1 runtime reliability, 3D diagnostics, navigation and source/visual acceptance remain open.

An earlier worker red-run artifact was incomplete (latest artifact 10/12 records), and its evolving timestamps/counts do not establish an unambiguous single-run provenance. It is not accepted as a full baseline or used as the before proof above. The final runner fixes the hidden-pane wait and client/server clock labels before green verification. No errors are suppressed and no acceptance threshold is lowered.
